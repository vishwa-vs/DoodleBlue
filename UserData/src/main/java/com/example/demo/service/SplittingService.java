package com.example.demo.service;

import org.springframework.stereotype.Service;

@Service
public class SplittingService {
	/*
	 * String array[]=null; public String[] splitter(String email) { String array[]
	 * = email.split(","); return array;
	 * 
	 * }
	 */
}
